cases:
    - name: test_all_linters_happy_path
      jobs.yaml: |
          - job:
              name: test-job
              builders:
                - shell: "#!/bin/sh -eux"
              wrappers:
                - timestamps
      expected_output: ""

    - name: test_missing_timestamps
      jobs.yaml: |
          - job:
              name: test-job
              builders:
                - shell: "#!/bin/sh -eux"
      expected_output: |
        test-job: checking for timestamps: FAIL
